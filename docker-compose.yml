services:
  news-bot:
    image: ghcr.io/foxy1402/telegram-news-summarizer:latest
    container_name: telegram-news-summarizer
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./news_data:/news_data
